<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme uses Material 3 DayNight theme for proper system UI handling -->
    <style name="Theme.OCRApp" parent="android:Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
