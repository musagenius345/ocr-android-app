<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for Compose app - Material 3 theming is handled in Compose -->
    <style name="Theme.OCRApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
